
name: QuilvianSystemBackendDevQuilvianDev
on:
  push:
    branches:
      - QuilvianDev  # Sesuaikan dengan branch utama Anda

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v4

    - name: Setup SSH Connection
      run: |
        mkdir -p ~/.ssh
        echo "${{ secrets.SSH_PRIVATE_KEY_BACKEND }}" > ~/.ssh/id_rsa
        chmod 600 ~/.ssh/id_rsa
        ssh-keyscan -H ${{ secrets.SSH_IP_BACKEND }} >> ~/.ssh/known_hosts

    - name: Deploy to Server
      run: |
        ssh ${{ secrets.SSH_USER_BACKEND }}@${{ secrets.SSH_IP_BACKEND }} << 'EOF'

          cd ~/QuilvianSystemBackendDev-QuilvianDev

          echo "Resetting local changes..."
          git reset --hard origin/QuilvianDev
          git clean -fd

          echo "Pulling latest changes..."
          git pull origin QuilvianDev

          IMAGE_NAME="quilvian-backend-quilviandev"
          CONTAINER_NAME="quilvian-container-quilviandev"

          DEPLOY_TIME=$(date +"%A, %d-%m-%Y %H:%M:%S") 

          echo "Building new Docker image..."
          docker build -t $IMAGE_NAME .

          echo "Stopping and removing old container..."
          docker stop $CONTAINER_NAME || true
          docker rm $CONTAINER_NAME || true

          echo "Checking if container exists..."
          if [ "$(docker ps -a -q -f name=$CONTAINER_NAME)" ]; then
            echo "Container exists, restarting..."
            docker restart $CONTAINER_NAME
          else
            echo "Container does not exist, creating a new one..."

            docker run -d --name $CONTAINER_NAME -p 5003:80 $IMAGE_NAME
          fi

          echo "Cleaning up unused Docker images..."
          docker system prune -af

          echo "Deployment completed!"


          # Kirim notifikasi ke Telegram setelah update
          curl -X POST https://api.telegram.org/bot7630899057:AAFqAUGUB7I5V9VGKXbcmoWmbYWvrdmnje8/sendMessage \

              -d chat_id=-1002437306112 \
              -d text="✅ Deployment Sukses! 🚀%0A
              🔹 *Docker Image:* $IMAGE_NAME%0A
              🔹 *Container:* $CONTAINER_NAME%0A
              📅 *Tanggal:* $DEPLOY_TIME%0A
              🛠️ Deployment dilakukan oleh Branch *QuilvianDev*%0A
              🔄 Container telah diperbarui dan berjalan dengan baik. 🎉"
        EOF
# name: QuilvianSystemBackendDevQuilvianDev
# on:
#   push:
#     branches:
#       - QuilvianDev  # Sesuaikan dengan branch utama Anda
      
# jobs:
#   deploy:
#     runs-on: ubuntu-latest

#     steps:
#     - name: Checkout Repository
#       uses: actions/checkout@v4

#     - name: Setup SSH Connection
#       run: |
#         mkdir -p ~/.ssh
#         echo "${{ secrets.SSH_PRIVATE_KEY_BACKEND }}" > ~/.ssh/id_rsa
#         chmod 600 ~/.ssh/id_rsa
#         ssh-keyscan -H ${{ secrets.SSH_IP_BACKEND }} >> ~/.ssh/known_hosts

#     - name: Deploy to Server
#       run: |
#         ssh ${{ secrets.SSH_USER_BACKEND }}@${{ secrets.SSH_IP_BACKEND }} << 'EOF'
#           cd ~/QuilvianSystemBackendDev-QuilvianDev

#           echo "Resetting local changes..."
#           git reset --hard origin/QuilvianDev
#           git clean -fd

#           echo "Pulling latest changes..."
#           git pull origin QuilvianDev

#           echo "Building new Docker image..."
#           docker build -t quilvian-backend-QuilvianDev .

#           echo "Stopping and removing old container..."
#           docker stop quilvian-container-QuilvianDev || true
#           docker rm quilvian-container-QuilvianDev || true

#           echo "Running new container..."
#           docker run -d --name quilvian-container-QuilvianDev -p 5003:80 quilvian-backend-QuilvianDev
#           # docker restart quilvian-container-QuilvianDev || docker run -d --name quilvian-container-QuilvianDev -p 5003:80 quilvian-backend-QuilvianDev

#           echo "Cleaning up unused Docker images..."
#           docker system prune -af

#           echo "Deployment completed!"
#         EOF
# # name: Build and Push Docker image to Docker Hub

# # on:
# #   push:
# #     branches:
# #       - QuilvianDev  # Workflow ini akan berjalan setiap ada perubahan di branch 'QuilvianDev'

# # jobs:
# #   build:
# #     runs-on: ubuntu-latest  # Workflow ini akan dijalankan di Ubuntu terbaru

# #     steps:
# #       # Step 1: Checkout repository
# #       - name: Checkout repository
# #         uses: actions/checkout@v2

# #       # Step 2: Login ke Docker Hub
# #       - name: Log in to Docker Hub
# #         uses: docker/login-action@v2
# #         with:
# #           username: "ikbalyuliyanto7"  # Gunakan Docker Hub username dari GitHub Secrets
# #           password: "dckr_pat_NGNArQqEZT9NiksMvXujctsIIWk"  # Gunakan Docker Hub password/token dari GitHub Secrets

# #       # Step 3: Build Docker image
# #       - name: Build Docker image
# #         run: |
# #           docker build -t ikbalyuliyanto7/quilvian-postgre-api:latest .

# #       # Step 4: Push Docker image to Docker Hub
# #       - name: Push Docker image
# #         run: |
# #           docker push ikbalyuliyanto7/quilvian-postgre-api:latest

